# 1.4 Implementar projeto hr-payroll (mock)

Classe `Payment` (não é entidade JPA, sem persistência ainda), `PaymentService` e `PaymentController` implementados. `PaymentService.getPayment` retorna um `Payment` com dados mockados (nome e renda diária fixos), sem chamar o hr-worker de verdade ainda.

---

#### English

`Payment` class (not a JPA entity, no persistence yet), `PaymentService` and `PaymentController` implemented. `PaymentService.getPayment` returns a `Payment` with mocked data (fixed name and daily income), without calling hr-worker for real yet.
