# 1.3 Criar projeto hr-payroll

Projeto hr-payroll criado (Spring Boot 2.3.4, Java 11), trazido do repositório de referência. GroupId e pacote corrigidos de `com.devsuperior` pra `com.camposcodes`, mesmo padrão aplicado no hr-worker. Nenhuma classe de negócio criada ainda, só o esqueleto do projeto rodando.

---

#### English

hr-payroll project created (Spring Boot 2.3.4, Java 11), brought in from the reference repository. GroupId and package fixed from `com.devsuperior` to `com.camposcodes`, same pattern applied to hr-worker. No business classes created yet, just the project skeleton running.
