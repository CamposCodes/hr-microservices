# 1.1 Criar projeto hr-worker

Projeto hr-worker criado (Spring Boot 2.3.4, Java 11), trazido do repositório de referência. Maven configurado e importado no IntelliJ, JDK 11 associada. Nenhuma classe de negócio criada ainda, só o esqueleto do projeto rodando.

---

#### English

hr-worker project created (Spring Boot 2.3.4, Java 11), brought in from the reference repository. Maven configured and imported in IntelliJ, JDK 11 assigned. No business classes created yet, just the project skeleton running.
